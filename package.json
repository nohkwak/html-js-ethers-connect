{
  "dependencies": {
    "browserify": "^17.0.0",
    "ethers": "^5.5.3",
    "watchify": "^4.0.0",
    "@klaytn/ethers-ext": "0.9.7-beta"
  },
  "scripts": {
    "build": "browserify index.js --standalone bundle -o ./dist/bundle.js",
    "watch": "watchify index.js --standalone bundle -o ./dist/bundle.js -v"
  },
  "devDependencies": {
    "babelify": "^10.0.0",
    "http-server": "^14.1.0"
  }
}
